doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  meta(http-equiv='X-UA-Compatible' content='ie=edge')
  link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css')
  title Document
  style.
    .card {
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
    max-width: 300px;
    margin: auto;
    text-align: center;
    }
    .title {
    color: grey;
    font-size: 18px;
    }
    button {
    border: none;
    outline: 0;
    display: inline-block;
    padding: 8px;
    color: white;
    background-color: #000;
    text-align: center;
    cursor: pointer;
    width: 100%;
    font-size: 18px;
    }
    a {
    text-decoration: none;
    font-size: 22px;
    color: black;
    }
    button:hover, a:hover {
    opacity: 0.7;
    }
.card
  p#note(style='color: green; display: none;') An Email has been sent to your registered email id
  h1 #{data.name}
  p.title #{data.roles} 
  p Street #{data.address.street}
  p Suite  #{data.address.suite}
  p City   #{data.address.city}
  p Zipcode #{data.address.zipcode}
  p Username #{data.username}
  p Email 
   span.mail #{data.email}

  div 
    a#reset(href='/updatePassword') Update Password
  p Phone #{data.phone}
//- script(src='js/profile.js')
script(src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.js")